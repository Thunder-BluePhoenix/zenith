version: "1"

jobs:
  verify:
    steps:
      - name: Echo platform
        run: echo "Zenith functional test OK"

      - name: Write a file
        run: echo "hello from zenith" > zenith_test_out.txt

      - name: Read the file back
        run: cat zenith_test_out.txt

      - name: Cleanup
        run: del zenith_test_out.txt
