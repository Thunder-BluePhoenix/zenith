jobs:
  firecracker_test:
    runs-on: alpine
    backend: firecracker
    steps:
      - name: Verify MicroVM Skeleton
        run: echo "This should fail on Windows but show Firecracker initialization logs."
